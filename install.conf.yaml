# dotbot configuration

- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
#    ~/.vim: vim

# - create:

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]